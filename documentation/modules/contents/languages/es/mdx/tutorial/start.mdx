import {
	BeyondImage,
	DocHeader,
	BeyondJS,
	Subtitle,
	Pretitle,
	NextLinks,
	Title,
	Link,
	ELink,
} from '@beyond/docs/components/html';
import { Code, CodeBox } from '@beyond/docs/code';

<DocHeader pretitle="Tutorial" title="Proyecto Web con Backend" />

<Title tag="h4">Antes de Empezar</Title>

> Este tutorial busca el flujo principal de desarrollo con **BeyondJS** y
> demostrar de manera clara los
> beneficios que este ofrece. El propósito de este tutorial es que los
> usuarios/programadores puedan aprender mientras practican, por tanto, los
> conceptos que definen la estructura de **BeyondJS** son abordados de forma general,
> focalizando en hacer un ejercicio rápido, conciso y claro. Para mayor entendimiento de
> los algunos conceptos, es necesario acceder a las secciones correspondientes
> en la documentación.

<Title tag="h3" text="Prerequisitos" />

Para ejecutar el presente tutorial se requiere:

-   Tener instalado Node.js 16.x
-   tener conocimientos básicos sobre la consola de comandos.

Adicionalmente, se recomienda usar <ELink href="https://code.visualstudio.com/">vscode</ELink> para
poder seguir el tutorial paso a paso. Sin embargo, se puede utilizar cualquier otro editor.

<Title tag="h2" text="Empezando" />

Lo primero que haremos será abrir el editor en la carpeta donde queramos mannejar nuestros proyectos.

<Code>code .</Code>

Ahora, podemos abrir el terminal integrado con `ctrl + shift + C` (or `cmd + backtick`) y ejecutar allí el siguiente comando
para crear nuestro proyecto y paquete:

<Code>npx @beyond-js/create-package@latest</Code>

> Advertencia: los comandos de BeyondJS en el CLI son interactivos y requieren una consola compatible para su correcto funcionamiento. Asegúrate de tener una consola actualizada antes de utilizarlos.

Este comando es interactivo y solicita algunos datos para definir nuestro paquete o proyecto.
Los datos que pide son los siguientes:

-   **Package specifier**: Corresponde al nombre de nuestro paquete, para efectos de este tutorial
    nuestro paquete se llamará: '@beyond-js/tutorial'.
-   **Title**: "Tutorial Web de BeyondJS"
-   **About**: Puedes agregar cualquier texto o ignorarlo y avanzar.
-   **Package type**: Corresponde al tipo de proyecto a crear, seleccionaremos `web-backend` que contiene
    los paquetes para implementar una conexión entre un servicio node y nuestro proyecto web por medio de websockets,
    como vermos más adelante. BeyondJS lo instalará de manera local y var a generar la estructura inicial de nuestro proyecto.
-   Preguntará si deseamos instalar las dependencias, seleccionamos que sí.

¡Listo! nuestro proyecto se ha creado. A continuación abriremos nuestro editor de texto en el directorio
donde ejecutamos el comando

Al abrirlo podremos ver que tenemos la siguiente estructura:

-   **beyond.json:** Archivo para manejar el servidor de desarrollo, gestionado por BeyondJS.
-   **packages.json:** Este archivo registra los proyectos a los que el DevServer tiene acceso,
    generalmente es gestionado de forma directa por <BeyondJS/>.
-   **tutorial**: La carpeta de nuestro paquete y proyecto.

    -   **package.json**: Archivo del paquete javascript.
    -   **/modules:** Carpeta en donde se crean los módulos del proyecto por defecto. Este valor es configurable,
        puedes leer más de ello en la sección de <Link href="/docs/packages/json">Configuración de Paquetes.</Link>
    -   **template:** Esta carpeta contiene todos los elementos necesarios para definir el template general del proyecto.

        -   **template.json:** Archivo que permite realizar ajustes en la configuración del template del proyecto.

> Si deseas puedes conocer las diferencias sobre los tipos de proyectos
> en <Link href="/docs/packages/types"> la sección correspondiente de la documentación.</Link>

A continuación, debemos iniciar <BeyondJS/>, para ello ejecutamos el siguiente comando:

<Code language="shell">beyond run --workspace 4000</Code>

Al ejecutarlo, debe aparecer el siguiente mensaje en la consola de comandos:

<Code language="shell">
	{`Follow the link to manage your packages with the workspace:
https://workspace.beyondjs.com?port=4000`}
</Code>

¡Listo! Ya podemos acceder a nuestro proyecto en <ELink href="http://localhost:6510/">http://localhost:6510/</ELink>

<BeyondImage src="/images/tutorial/first-page.png" alt="your first page with BeyondJS" />

A continuación vamos a crear nuestro primer módulo.

<NextLinks items={['tutorial/first-module', 'tutorial/backend']} />
