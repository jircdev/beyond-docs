import { NextLinks, DocHeader, BeyondJS, Subtitle, Title, Link } from "@beyond/docs/components/html";
import { Code, CodeBox } from "@beyond/docs/code";

<Title>Module configuration</Title>.

The module configuration file is module.json.

<Code>
{`
{
  "name": "home",
  "platforms": [
    "backend",
    "web"
  ],
  "widget": {
    "hmr": true,
    "element": {
      "name": "home-page"
    },
    "name": "home",
    "route": "/${content}",
    "is": "page",
    "ts": {
      "path": "/widget/ts",
      "files": [
        "*"
      ]
    }
  }
}

`}

</Code>

The configuration properties are:

-   **name** [string] : Defines the name of the module, necessary for the elements to be imported.
-   platforms\*\* [array] : Defines the platforms supported by the module. The supported values are the identifiers of the existing <Link href="/concepts/platforms">platforms</Link>.

<Code>
{`
"platforms": [
    "backend",
    "web"
 ]

`}

</Code>

-   [bundle] <Link href="/module#bundle">[bundle]</Link> : Refiere al nombre identificador del tipo de bundle que se desea agregar en el módulo. Un módulo puede poseer uno o varios bundles. Cada bundle recibe un objeto de configuración con los procesadores que utiliza y alguna variación inherita del tipo de bundle agregado.

<NextLinks items={["backend", "widgets"]} />
