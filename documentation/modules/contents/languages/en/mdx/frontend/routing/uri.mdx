import { Link, ELink, NextLinks, DocHeader, BeyondJS, Title, Subtitle } from "@beyond/docs/components/html";
import { Code, CodeBox } from "@beyond/docs/code";

<DocHeader pretitle="Frontend" title="Objeto URI" />

Importación

<Code>{`import {routing: {uri}} from "@beyond-js/kernel/routing";`}</Code>

The uri object provides an API equivalent to that of the objetc `window.location`, but with added functionalities that ensure correct behavior in both web and mobile environments.

<Title tag="h3" text="Properties" />

-   **pathname string** : current url, without querystring or hash
-   **uri string** : full url.
-   **qs Map** : Map with the list of variables passed through the querystring.
-   **hashtag string** : contains the fragment of the url after the '#'. It is equivalent to the hash property of the location object of the Web API.
-   **vars Map** : Map container of the dynamic variables added in the definition of the url of the <Link href="/widgets#page">Widget Page.</Link>

<Title tag="h2" text="routing.historyBeyondHistory" />

The BeyondHistory object handles all information associated with the user's navigation in the current session.

<Title tag="h4" text="Properties" />

-   **current**: string : current navigation url.
-   **initial**: number : Refers to the index associated with the browser web API history on which navigation within the
    BeyondJS project begins. For this management, Beyond uses the SessionStorage object of the web API, where it records two properties:

        -   \_\_beyond_navigation_position
        -   \_\_beyond_navigation_records

-   **position** HistoryPosition :
    -   **value** number : value of the current navigation position in the BeyondJS own history.
-   **records** HistoryRecords :: Map object that contains all user navigation entries in the current session.
