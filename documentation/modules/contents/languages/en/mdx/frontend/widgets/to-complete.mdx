### Casos de estudio.

-   Puede darse el caso donde el programador necesita saber que el componente se monta.
-   Si deseas construir un controlador para renderizar HTML puro o sin framework de vistas.
-   Crear un controlador para alguien que desea trabajar con un framework de vista.

## Notas

Los atributos deben definirse en el module.json pues deben ejecutarse en el constructor del Componente Web.
Las propiedades permiten que Beyond Identifique que elemento se esta renderizando en sistemas de
renderizado SSR y SR.

los atributesChanges son custom atrributes.

# documentar

propiedad `widget` es el Web Componente

# noDocumentar
